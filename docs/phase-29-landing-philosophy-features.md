# Phase 29 — Landing page, step 2: philosophy and features

- `Philosophy.tsx`: the non-punitive idea shown as a week strip where the rest day is a neutral moon, not a failure, next to the "streak hangus" contrast and the 5-habit cap.
- `Features.tsx`: bento grid with exactly 7 cells for 7 features (`md:grid-cols-6`, spans collapse to one column on mobile). Cells vary in background (lime gradient, dark, grid pattern, lime tint) instead of seven identical cards.
- Copy was checked against the code: CSV export is free and only PDF is Plus; the personal weekly challenge from AI is Plus; badge milestones run from 3 days to a year.
