# Phase 35 — Empty states and welcome tips

- `components/EmptyState.tsx`: reusable placeholder (floating icon, one-line explanation, first-step button). Used for Today, Habit Tracker, and both Goals lists.
- `components/WelcomeTips.tsx`: dismissible three-tip card on the dashboard (check in, Catat AI, ask the Coach). Dismissal is stored in `localStorage` (`continuum_tips_dismissed`).
