# Phase 23 — UI polish, step 3: Today + HabitCard

- HabitCard: check button pops with an expanding lime ring only at the moment of check-in
  (cards that load already done stay still), streak flame flickers, card lifts on hover.
  Touch target on mobile raised to 40px.
- Today: staggered entry of columns and habit cards, animated daily progress bar
  (`done/total`, transform-only), bar chart bars grow from the baseline, waving hand,
  AI draft / error banners fade in, buttons get press feedback, quick-add bar glows on focus.
- New keyframes `grow-y`, `wave`.
