# Phase 26 — UI polish, step 6: Plus, Account, Settings, auth and onboarding

- `shadow-soft` / `shadow-lift` are now Tailwind `boxShadow` tokens (tinted olive), so variants like `focus:shadow-lift` work.
- Plus / Account / Settings: sections stagger in, account stat tiles lift, hero cards use a deeper tinted shadow.
- Login / Register: drifting lime and lavender glow behind a card that scales in, logo pops, `min-h-[100dvh]` for mobile browsers.
- Onboarding: every step slides up, progress dots animate width via a spring transition, choice rows get press feedback.
