# Phase 24 — UI polish, step 4: Goals and Habit Tracker

- Goal and habit cards use the shared `lift` hover (rise + tinted shadow, hover-capable pointers only).
- Goal sections and the habit grid stagger in; primary buttons use `press` feedback instead of a scale-on-hover that jittered on touch.
- Card shadows switched to the tinted `shadow-soft`; the goal detail page follows.
