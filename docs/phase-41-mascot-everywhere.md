# Phase 41 — Mascot in empty states and on the landing page

- `EmptyState` takes an optional `mascot` mood. Habit lists use a happy Conti, the empty goals list a sleepy one.
- Landing: Conti sits on top of the phone mock and cheers when all three demo habits are checked (tap it to make it hop).
