# Phase 36 — PWA shortcuts

- `manifest.json`: long-press shortcuts on the installed app icon now cover Hari ini, Habit Tracker, Catat aktivitas and Coach AI; splash background matches the dark brand colour; app categories added.
- Service worker cache version bumped to `v3` so installed apps drop the old cached shell and pick up the new landing page, theme and manifest.

Not done: user-configurable reminder time (needs a backend field and scheduler change), Google sign-in (needs OAuth client setup), data import.
