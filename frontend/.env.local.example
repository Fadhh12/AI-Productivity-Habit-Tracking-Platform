# Base URL of the Continuum backend API.
# For local dev when opening the app from a phone on the same Wi-Fi, use your
# computer's LAN IP instead of localhost, e.g. http://192.168.1.23:3000
NEXT_PUBLIC_API_URL=http://localhost:3000
